import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const SignOut = () => {
  return (
    <View>
      <Text style={{fontSize: 50, textAlign: 'center'}}>SignOut</Text>
    </View>
  );
};

export default SignOut;

const styles = StyleSheet.create({});
